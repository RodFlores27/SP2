/**
 * Re-export so imports without an extension resolve from `.js` callers (e.g. dashboard utils).
 * Catalog lives in `bookingMessages.jsx` (JSX fragments / Lucide icon refs).
 */
export { bookingMessages, default } from './bookingMessages.jsx';
