# Deployment (redirect)

**Single canonical guide:** [RENDER-SUPABASE-DEPLOYMENT.md](./RENDER-SUPABASE-DEPLOYMENT.md)

That document covers Vercel (frontend), Render (backend), Supabase (PostgreSQL), UptimeRobot, env vars, CORS, troubleshooting, and optional paths (e.g. ngrok for short demos).

This file is kept so old links and bookmarks still work.
